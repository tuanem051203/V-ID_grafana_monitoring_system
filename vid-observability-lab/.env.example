# Bootstrap configuration only. The renderer selects APP_ENV from config.json.
LOG_LEVEL=INFO

# Supply real values outside Git. Compose mounts these values as secret files.
GRAFANA_ADMIN_PASSWORD=admin
VID_SMTP_APP_PASSWORD=wfnk azzw ktkf lxaf
